android.useAndroidX=true

# includeBuild does not inherit the app's gradle.properties. Without this,
# ReactAndroid compiles every ABI (~7GB extra) and fills the disk.
reactNativeArchitectures=arm64-v8a